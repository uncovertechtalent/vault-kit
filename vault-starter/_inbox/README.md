Everything unsorted lands here: pasted chats, meeting notes, half-thoughts, links.

A periodic atomize pass (run `/atomize` on this folder, or ask the agent to sweep it) turns entries into atoms and files them. The inbox shrinking to zero regularly is the health metric; an inbox that only grows means the vault is becoming a pile.
